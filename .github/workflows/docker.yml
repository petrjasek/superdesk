name: "Docker"

on:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - run: docker compose build superdesk-server
      timeout-minutes: 10

    - run: docker compose build superdesk-client
      timeout-minutes: 20
